<%= form_with(model: sport, local: true) do |form| %>
  <% if sport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sport.errors.count, "error") %> prohibited this sport from being saved:</h2>

      <ul>
      <% sport.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sport_name %>
    <%= form.text_field :sport_name %>
  </div>

  <div class="field">
    <%= form.label :total_player %>
    <%= form.number_field :total_player %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
